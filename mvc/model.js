class SlotModel {
  constructor() {
      this.balance = 100;
      this.betAmount = 5;
      this.totalWin = 0;
      this.running = false;
  }

  spin() {
      // Logic for spinning
  }

  calculateWin(reels) {
      // Logic for calculating win
  }
}

export default SlotModel;
